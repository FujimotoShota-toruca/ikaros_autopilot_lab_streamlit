# IKAROS-GO! v12（本格・決定論）

この版は「本格っぽさ」を優先した模型です。

- **状態遷移行列（感度行列）**っぽいもの `C(k)` を使って、B-plane上の位置を動かします
- **発電（太陽）**と **通信（地球）** を、姿勢（β_in/out）から判定します
- **乱数なし**：同じ操作なら同じ結果になります（再現性100%）

Build: v14-results-physics-orbit-2026-02-14

## 起動方法
```bash
pip install -r requirements.txt
streamlit run app.py
```

## ざっくり数式（模型）
```
x_(k+1) = x_k + drift(k) + k · C(k) · [β_in, β_out]^T
```
- drift(k)：β=0でもズレる（決定論的）
- k：効きの誤差（真値k_trueと想定k_hat）
- 通信できたら推定が一気に良くなる（測位アップデートの模型）


## デフォルトIKAROS画像
`assets/ikaros_texture.png` を同梱しています。3Dタブで画像をアップしない場合、この画像が使われます。


## v14 変更点
- 太陽系2D：地球・金星の円軌道 + IKAROSのホーマン遷移っぽい楕円（見た目を安定）
- B-plane：これまでの点を線でつないで表示
- 点数：距離（ダーツ）に加えて、発電/通信を点数に入れるかを切り替え可能
- ゲーム終了後：リザルト画面に自動で移動（各種グラフ + もう一度ボタン）
